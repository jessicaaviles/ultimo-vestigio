services:
  - type: web
    name: jogo-investigacao-kobn
    env: node
    rootDir: backend
    buildCommand: npm ci && npm run build
    startCommand: npm run db:sync-auth && npm start
    healthCheckPath: /health
